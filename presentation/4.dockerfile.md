# Dockerfile

4.1 Building Production-Ready Dockerfiles
Slim base images
Non-root users
Multi-stage builds
Pinning versions
Reproducible builds
Reducing attack surface

4.2 Dockerfile Best Practices
COPY vs ADD
Layer ordering for caching
Structuring RUN commands
Using .dockerignore
Environment variables and ARGs

4.3 Multi-Stage Builds
Builder stage vs runtime stage
Reducing image size
Keeping artifacts clean

4.4 Docker BuildKit Deep Dive
Parallel builds
Secrets & SSH mounts
Inline cache
Build attestation
BuildKit features vs classic builder
